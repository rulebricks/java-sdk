/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.rulebricks.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.rulebricks.core.ObjectMappers;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = ImportManifestRequest.Builder.class
)
public final class ImportManifestRequest {
  private final ImportManifestRequestManifest manifest;

  private final Optional<ImportManifestRequestConflictStrategy> conflictStrategy;

  private final Optional<String> targetFolderName;

  private final Optional<String> parentFolderId;

  private final Optional<Boolean> clearHistory;

  private final Optional<Boolean> previewOnly;

  private final Map<String, Object> additionalProperties;

  private ImportManifestRequest(ImportManifestRequestManifest manifest,
      Optional<ImportManifestRequestConflictStrategy> conflictStrategy,
      Optional<String> targetFolderName, Optional<String> parentFolderId,
      Optional<Boolean> clearHistory, Optional<Boolean> previewOnly,
      Map<String, Object> additionalProperties) {
    this.manifest = manifest;
    this.conflictStrategy = conflictStrategy;
    this.targetFolderName = targetFolderName;
    this.parentFolderId = parentFolderId;
    this.clearHistory = clearHistory;
    this.previewOnly = previewOnly;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The RBM manifest object containing assets to import. Asset objects inside the manifest intentionally preserve <code>.rbm</code>/database casing so exported manifests can be imported without rewriting asset payloads. A compressed manifest is also accepted: the JSON array produced by the compress-json library (for example, the contents of a compressed .rbm file exported with <code>compress: true</code>); it is detected and decompressed automatically.
   */
  @JsonProperty("manifest")
  public ImportManifestRequestManifest getManifest() {
    return manifest;
  }

  /**
   * @return How to handle assets in the manifest that already exist in the workspace (matched by stable ID, or by name for contexts). 'override' replaces them in place, keeping their workspace ID, slug, folder and access groups but taking the manifest's content and version history (existing versions and their release pins are dropped; contexts are deleted with their stored records and recreated). 'preserve' keeps existing assets unchanged and reuses them for new dependents. 'block' rejects the whole import before any write if anything already exists. Object-managed values are never written.
   */
  @JsonProperty("conflict_strategy")
  public Optional<ImportManifestRequestConflictStrategy> getConflictStrategy() {
    return conflictStrategy;
  }

  /**
   * @return Optional folder name to place imported assets into. Created if it doesn't exist.
   */
  @JsonProperty("target_folder_name")
  public Optional<String> getTargetFolderName() {
    return targetFolderName;
  }

  /**
   * @return Optional parent folder for imported asset folders.
   */
  @JsonProperty("parent_folder_id")
  public Optional<String> getParentFolderId() {
    return parentFolderId;
  }

  /**
   * @return Clear imported edit history, typically for templates.
   */
  @JsonProperty("clear_history")
  public Optional<Boolean> getClearHistory() {
    return clearHistory;
  }

  /**
   * @return Return the server-authoritative create/reuse/reject plan without writing.
   */
  @JsonProperty("preview_only")
  public Optional<Boolean> getPreviewOnly() {
    return previewOnly;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof ImportManifestRequest && equalTo((ImportManifestRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(ImportManifestRequest other) {
    return manifest.equals(other.manifest) && conflictStrategy.equals(other.conflictStrategy) && targetFolderName.equals(other.targetFolderName) && parentFolderId.equals(other.parentFolderId) && clearHistory.equals(other.clearHistory) && previewOnly.equals(other.previewOnly);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.manifest, this.conflictStrategy, this.targetFolderName, this.parentFolderId, this.clearHistory, this.previewOnly);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static ManifestStage builder() {
    return new Builder();
  }

  public interface ManifestStage {
    /**
     * <p>The RBM manifest object containing assets to import. Asset objects inside the manifest intentionally preserve <code>.rbm</code>/database casing so exported manifests can be imported without rewriting asset payloads. A compressed manifest is also accepted: the JSON array produced by the compress-json library (for example, the contents of a compressed .rbm file exported with <code>compress: true</code>); it is detected and decompressed automatically.</p>
     */
    _FinalStage manifest(@NotNull ImportManifestRequestManifest manifest);

    Builder from(ImportManifestRequest other);
  }

  public interface _FinalStage {
    ImportManifestRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>How to handle assets in the manifest that already exist in the workspace (matched by stable ID, or by name for contexts). 'override' replaces them in place, keeping their workspace ID, slug, folder and access groups but taking the manifest's content and version history (existing versions and their release pins are dropped; contexts are deleted with their stored records and recreated). 'preserve' keeps existing assets unchanged and reuses them for new dependents. 'block' rejects the whole import before any write if anything already exists. Object-managed values are never written.</p>
     */
    _FinalStage conflictStrategy(Optional<ImportManifestRequestConflictStrategy> conflictStrategy);

    _FinalStage conflictStrategy(ImportManifestRequestConflictStrategy conflictStrategy);

    /**
     * <p>Optional folder name to place imported assets into. Created if it doesn't exist.</p>
     */
    _FinalStage targetFolderName(Optional<String> targetFolderName);

    _FinalStage targetFolderName(String targetFolderName);

    /**
     * <p>Optional parent folder for imported asset folders.</p>
     */
    _FinalStage parentFolderId(Optional<String> parentFolderId);

    _FinalStage parentFolderId(String parentFolderId);

    /**
     * <p>Clear imported edit history, typically for templates.</p>
     */
    _FinalStage clearHistory(Optional<Boolean> clearHistory);

    _FinalStage clearHistory(Boolean clearHistory);

    /**
     * <p>Return the server-authoritative create/reuse/reject plan without writing.</p>
     */
    _FinalStage previewOnly(Optional<Boolean> previewOnly);

    _FinalStage previewOnly(Boolean previewOnly);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements ManifestStage, _FinalStage {
    private ImportManifestRequestManifest manifest;

    private Optional<Boolean> previewOnly = Optional.empty();

    private Optional<Boolean> clearHistory = Optional.empty();

    private Optional<String> parentFolderId = Optional.empty();

    private Optional<String> targetFolderName = Optional.empty();

    private Optional<ImportManifestRequestConflictStrategy> conflictStrategy = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(ImportManifestRequest other) {
      manifest(other.getManifest());
      conflictStrategy(other.getConflictStrategy());
      targetFolderName(other.getTargetFolderName());
      parentFolderId(other.getParentFolderId());
      clearHistory(other.getClearHistory());
      previewOnly(other.getPreviewOnly());
      return this;
    }

    /**
     * <p>The RBM manifest object containing assets to import. Asset objects inside the manifest intentionally preserve <code>.rbm</code>/database casing so exported manifests can be imported without rewriting asset payloads. A compressed manifest is also accepted: the JSON array produced by the compress-json library (for example, the contents of a compressed .rbm file exported with <code>compress: true</code>); it is detected and decompressed automatically.</p>
     * <p>The RBM manifest object containing assets to import. Asset objects inside the manifest intentionally preserve <code>.rbm</code>/database casing so exported manifests can be imported without rewriting asset payloads. A compressed manifest is also accepted: the JSON array produced by the compress-json library (for example, the contents of a compressed .rbm file exported with <code>compress: true</code>); it is detected and decompressed automatically.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("manifest")
    public _FinalStage manifest(@NotNull ImportManifestRequestManifest manifest) {
      this.manifest = Objects.requireNonNull(manifest, "manifest must not be null");
      return this;
    }

    /**
     * <p>Return the server-authoritative create/reuse/reject plan without writing.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage previewOnly(Boolean previewOnly) {
      this.previewOnly = Optional.ofNullable(previewOnly);
      return this;
    }

    /**
     * <p>Return the server-authoritative create/reuse/reject plan without writing.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "preview_only",
        nulls = Nulls.SKIP
    )
    public _FinalStage previewOnly(Optional<Boolean> previewOnly) {
      this.previewOnly = previewOnly;
      return this;
    }

    /**
     * <p>Clear imported edit history, typically for templates.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage clearHistory(Boolean clearHistory) {
      this.clearHistory = Optional.ofNullable(clearHistory);
      return this;
    }

    /**
     * <p>Clear imported edit history, typically for templates.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "clear_history",
        nulls = Nulls.SKIP
    )
    public _FinalStage clearHistory(Optional<Boolean> clearHistory) {
      this.clearHistory = clearHistory;
      return this;
    }

    /**
     * <p>Optional parent folder for imported asset folders.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage parentFolderId(String parentFolderId) {
      this.parentFolderId = Optional.ofNullable(parentFolderId);
      return this;
    }

    /**
     * <p>Optional parent folder for imported asset folders.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "parent_folder_id",
        nulls = Nulls.SKIP
    )
    public _FinalStage parentFolderId(Optional<String> parentFolderId) {
      this.parentFolderId = parentFolderId;
      return this;
    }

    /**
     * <p>Optional folder name to place imported assets into. Created if it doesn't exist.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage targetFolderName(String targetFolderName) {
      this.targetFolderName = Optional.ofNullable(targetFolderName);
      return this;
    }

    /**
     * <p>Optional folder name to place imported assets into. Created if it doesn't exist.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "target_folder_name",
        nulls = Nulls.SKIP
    )
    public _FinalStage targetFolderName(Optional<String> targetFolderName) {
      this.targetFolderName = targetFolderName;
      return this;
    }

    /**
     * <p>How to handle assets in the manifest that already exist in the workspace (matched by stable ID, or by name for contexts). 'override' replaces them in place, keeping their workspace ID, slug, folder and access groups but taking the manifest's content and version history (existing versions and their release pins are dropped; contexts are deleted with their stored records and recreated). 'preserve' keeps existing assets unchanged and reuses them for new dependents. 'block' rejects the whole import before any write if anything already exists. Object-managed values are never written.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage conflictStrategy(ImportManifestRequestConflictStrategy conflictStrategy) {
      this.conflictStrategy = Optional.ofNullable(conflictStrategy);
      return this;
    }

    /**
     * <p>How to handle assets in the manifest that already exist in the workspace (matched by stable ID, or by name for contexts). 'override' replaces them in place, keeping their workspace ID, slug, folder and access groups but taking the manifest's content and version history (existing versions and their release pins are dropped; contexts are deleted with their stored records and recreated). 'preserve' keeps existing assets unchanged and reuses them for new dependents. 'block' rejects the whole import before any write if anything already exists. Object-managed values are never written.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "conflict_strategy",
        nulls = Nulls.SKIP
    )
    public _FinalStage conflictStrategy(
        Optional<ImportManifestRequestConflictStrategy> conflictStrategy) {
      this.conflictStrategy = conflictStrategy;
      return this;
    }

    @java.lang.Override
    public ImportManifestRequest build() {
      return new ImportManifestRequest(manifest, conflictStrategy, targetFolderName, parentFolderId, clearHistory, previewOnly, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
