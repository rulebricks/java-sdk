/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.rulebricks.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.rulebricks.core.Nullable;
import com.rulebricks.core.NullableNonemptyFilter;
import com.rulebricks.core.ObjectMappers;
import java.lang.Integer;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = ScaleStatusResponse.Builder.class
)
public final class ScaleStatusResponse {
  private final ScaleStatusResponseStatus status;

  private final Optional<Integer> activeWorkers;

  private final int targetWorkers;

  private final int expiresInSeconds;

  private final Map<String, Object> additionalProperties;

  private ScaleStatusResponse(ScaleStatusResponseStatus status, Optional<Integer> activeWorkers,
      int targetWorkers, int expiresInSeconds, Map<String, Object> additionalProperties) {
    this.status = status;
    this.activeWorkers = activeWorkers;
    this.targetWorkers = targetWorkers;
    this.expiresInSeconds = expiresInSeconds;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return <code>idle</code>: no warm-up is active. <code>scaling</code>: a warm-up is active and workers are still joining. <code>ready</code>: the active worker count reached the target.
   */
  @JsonProperty("status")
  public ScaleStatusResponseStatus getStatus() {
    return status;
  }

  /**
   * @return Solvers currently joined to the processing group and able to accept work. Null when the deployment runs without a message broker (no autoscaling).
   */
  @JsonIgnore
  public Optional<Integer> getActiveWorkers() {
    if (activeWorkers == null) {
      return Optional.empty();
    }
    return activeWorkers;
  }

  /**
   * @return The fleet ceiling a warm-up scales toward - a function of the deployment's configuration, never caller input.
   */
  @JsonProperty("target_workers")
  public int getTargetWorkers() {
    return targetWorkers;
  }

  /**
   * @return Seconds until the active warm-up lapses and normal autoscaling reclaims the capacity. 0 when idle. Repeat POST /scale calls refresh the window.
   */
  @JsonProperty("expires_in_seconds")
  public int getExpiresInSeconds() {
    return expiresInSeconds;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("active_workers")
  private Optional<Integer> _getActiveWorkers() {
    return activeWorkers;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof ScaleStatusResponse && equalTo((ScaleStatusResponse) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(ScaleStatusResponse other) {
    return status.equals(other.status) && activeWorkers.equals(other.activeWorkers) && targetWorkers == other.targetWorkers && expiresInSeconds == other.expiresInSeconds;
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.status, this.activeWorkers, this.targetWorkers, this.expiresInSeconds);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static StatusStage builder() {
    return new Builder();
  }

  public interface StatusStage {
    /**
     * <p><code>idle</code>: no warm-up is active. <code>scaling</code>: a warm-up is active and workers are still joining. <code>ready</code>: the active worker count reached the target.</p>
     */
    TargetWorkersStage status(@NotNull ScaleStatusResponseStatus status);

    Builder from(ScaleStatusResponse other);
  }

  public interface TargetWorkersStage {
    /**
     * <p>The fleet ceiling a warm-up scales toward - a function of the deployment's configuration, never caller input.</p>
     */
    ExpiresInSecondsStage targetWorkers(int targetWorkers);
  }

  public interface ExpiresInSecondsStage {
    /**
     * <p>Seconds until the active warm-up lapses and normal autoscaling reclaims the capacity. 0 when idle. Repeat POST /scale calls refresh the window.</p>
     */
    _FinalStage expiresInSeconds(int expiresInSeconds);
  }

  public interface _FinalStage {
    ScaleStatusResponse build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Solvers currently joined to the processing group and able to accept work. Null when the deployment runs without a message broker (no autoscaling).</p>
     */
    _FinalStage activeWorkers(Optional<Integer> activeWorkers);

    _FinalStage activeWorkers(Integer activeWorkers);

    _FinalStage activeWorkers(Nullable<Integer> activeWorkers);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements StatusStage, TargetWorkersStage, ExpiresInSecondsStage, _FinalStage {
    private ScaleStatusResponseStatus status;

    private int targetWorkers;

    private int expiresInSeconds;

    private Optional<Integer> activeWorkers = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(ScaleStatusResponse other) {
      status(other.getStatus());
      activeWorkers(other.getActiveWorkers());
      targetWorkers(other.getTargetWorkers());
      expiresInSeconds(other.getExpiresInSeconds());
      return this;
    }

    /**
     * <p><code>idle</code>: no warm-up is active. <code>scaling</code>: a warm-up is active and workers are still joining. <code>ready</code>: the active worker count reached the target.</p>
     * <p><code>idle</code>: no warm-up is active. <code>scaling</code>: a warm-up is active and workers are still joining. <code>ready</code>: the active worker count reached the target.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("status")
    public TargetWorkersStage status(@NotNull ScaleStatusResponseStatus status) {
      this.status = Objects.requireNonNull(status, "status must not be null");
      return this;
    }

    /**
     * <p>The fleet ceiling a warm-up scales toward - a function of the deployment's configuration, never caller input.</p>
     * <p>The fleet ceiling a warm-up scales toward - a function of the deployment's configuration, never caller input.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("target_workers")
    public ExpiresInSecondsStage targetWorkers(int targetWorkers) {
      this.targetWorkers = targetWorkers;
      return this;
    }

    /**
     * <p>Seconds until the active warm-up lapses and normal autoscaling reclaims the capacity. 0 when idle. Repeat POST /scale calls refresh the window.</p>
     * <p>Seconds until the active warm-up lapses and normal autoscaling reclaims the capacity. 0 when idle. Repeat POST /scale calls refresh the window.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("expires_in_seconds")
    public _FinalStage expiresInSeconds(int expiresInSeconds) {
      this.expiresInSeconds = expiresInSeconds;
      return this;
    }

    /**
     * <p>Solvers currently joined to the processing group and able to accept work. Null when the deployment runs without a message broker (no autoscaling).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage activeWorkers(Nullable<Integer> activeWorkers) {
      if (activeWorkers.isNull()) {
        this.activeWorkers = null;
      }
      else if (activeWorkers.isEmpty()) {
        this.activeWorkers = Optional.empty();
      }
      else {
        this.activeWorkers = Optional.of(activeWorkers.get());
      }
      return this;
    }

    /**
     * <p>Solvers currently joined to the processing group and able to accept work. Null when the deployment runs without a message broker (no autoscaling).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage activeWorkers(Integer activeWorkers) {
      this.activeWorkers = Optional.ofNullable(activeWorkers);
      return this;
    }

    /**
     * <p>Solvers currently joined to the processing group and able to accept work. Null when the deployment runs without a message broker (no autoscaling).</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "active_workers",
        nulls = Nulls.SKIP
    )
    public _FinalStage activeWorkers(Optional<Integer> activeWorkers) {
      this.activeWorkers = activeWorkers;
      return this;
    }

    @java.lang.Override
    public ScaleStatusResponse build() {
      return new ScaleStatusResponse(status, activeWorkers, targetWorkers, expiresInSeconds, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
