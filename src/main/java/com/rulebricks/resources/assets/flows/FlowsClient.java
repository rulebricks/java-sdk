/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.rulebricks.resources.assets.flows;

import com.rulebricks.core.ClientOptions;
import com.rulebricks.core.RequestOptions;
import com.rulebricks.resources.assets.flows.requests.DeleteFlowRequest;
import com.rulebricks.resources.assets.flows.requests.ImportFlowRequest;
import com.rulebricks.resources.assets.flows.requests.ListFlowsRequest;
import com.rulebricks.resources.assets.flows.requests.PullFlowsRequest;
import com.rulebricks.types.FlowDetail;
import com.rulebricks.types.FlowImportPayload;
import com.rulebricks.types.FlowImportResponse;
import com.rulebricks.types.SuccessMessage;
import java.util.List;

public class FlowsClient {
  protected final ClientOptions clientOptions;

  private final RawFlowsClient rawClient;

  public FlowsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawFlowsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawFlowsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * List flows in the organization, scoped to the API key holder's user groups. Combine folder, labels, user_group, id, slug, name, and search filters. When version is supplied, the filters must match exactly one accessible flow: multiple matches return 400 and no matches return 404. Version accepts a published version number, release environment slug, or latest, using the same publication and access checks as execution. A missing version or release returns 404. The response remains an array; request_schema and origin_rule come from the selected graph, while descriptive workspace metadata stays current. Without version, published flows use their published graph and unpublished flows use their draft graph. Flows do not declare a response schema.
   */
  public List<FlowDetail> list() {
    return this.rawClient.list().body();
  }

  /**
   * List flows in the organization, scoped to the API key holder's user groups. Combine folder, labels, user_group, id, slug, name, and search filters. When version is supplied, the filters must match exactly one accessible flow: multiple matches return 400 and no matches return 404. Version accepts a published version number, release environment slug, or latest, using the same publication and access checks as execution. A missing version or release returns 404. The response remains an array; request_schema and origin_rule come from the selected graph, while descriptive workspace metadata stays current. Without version, published flows use their published graph and unpublished flows use their draft graph. Flows do not declare a response schema.
   */
  public List<FlowDetail> list(RequestOptions requestOptions) {
    return this.rawClient.list(requestOptions).body();
  }

  /**
   * List flows in the organization, scoped to the API key holder's user groups. Combine folder, labels, user_group, id, slug, name, and search filters. When version is supplied, the filters must match exactly one accessible flow: multiple matches return 400 and no matches return 404. Version accepts a published version number, release environment slug, or latest, using the same publication and access checks as execution. A missing version or release returns 404. The response remains an array; request_schema and origin_rule come from the selected graph, while descriptive workspace metadata stays current. Without version, published flows use their published graph and unpublished flows use their draft graph. Flows do not declare a response schema.
   */
  public List<FlowDetail> list(ListFlowsRequest request) {
    return this.rawClient.list(request).body();
  }

  /**
   * List flows in the organization, scoped to the API key holder's user groups. Combine folder, labels, user_group, id, slug, name, and search filters. When version is supplied, the filters must match exactly one accessible flow: multiple matches return 400 and no matches return 404. Version accepts a published version number, release environment slug, or latest, using the same publication and access checks as execution. A missing version or release returns 404. The response remains an array; request_schema and origin_rule come from the selected graph, while descriptive workspace metadata stays current. Without version, published flows use their published graph and unpublished flows use their draft graph. Flows do not declare a response schema.
   */
  public List<FlowDetail> list(ListFlowsRequest request, RequestOptions requestOptions) {
    return this.rawClient.list(request, requestOptions).body();
  }

  /**
   * Create or update a flow from the Rulebricks Flow Schema (a list of <code>nodes</code> and <code>connections</code>). The server expands the Rulebricks Flow Schema definition into the full flow graph - laying it out, wiring property/control handles, resolving referenced published rules, and backfilling node defaults - so the result both renders in the editor and executes via <code>/flows/{slug}</code> without any manual editing. If <code>id</code> is provided the matching flow is updated; otherwise a new flow is created (<code>id</code>/<code>slug</code> auto-generated). Flows auto-publish unless <code>_publish</code> is set to <code>false</code>.
   */
  public FlowImportResponse push(ImportFlowRequest request) {
    return this.rawClient.push(request).body();
  }

  /**
   * Create or update a flow from the Rulebricks Flow Schema (a list of <code>nodes</code> and <code>connections</code>). The server expands the Rulebricks Flow Schema definition into the full flow graph - laying it out, wiring property/control handles, resolving referenced published rules, and backfilling node defaults - so the result both renders in the editor and executes via <code>/flows/{slug}</code> without any manual editing. If <code>id</code> is provided the matching flow is updated; otherwise a new flow is created (<code>id</code>/<code>slug</code> auto-generated). Flows auto-publish unless <code>_publish</code> is set to <code>false</code>.
   */
  public FlowImportResponse push(ImportFlowRequest request, RequestOptions requestOptions) {
    return this.rawClient.push(request, requestOptions).body();
  }

  /**
   * Export a flow into the Rulebricks Flow Schema (nodes + connections), the same shape accepted by <code>/admin/flows/import</code>. Works for flows built entirely by hand in the editor, so they can be round-tripped or version-controlled. This is distinct from the top-level <code>/admin/export</code>, which produces <code>.rbm</code> manifests.
   */
  public FlowImportPayload pull() {
    return this.rawClient.pull().body();
  }

  /**
   * Export a flow into the Rulebricks Flow Schema (nodes + connections), the same shape accepted by <code>/admin/flows/import</code>. Works for flows built entirely by hand in the editor, so they can be round-tripped or version-controlled. This is distinct from the top-level <code>/admin/export</code>, which produces <code>.rbm</code> manifests.
   */
  public FlowImportPayload pull(RequestOptions requestOptions) {
    return this.rawClient.pull(requestOptions).body();
  }

  /**
   * Export a flow into the Rulebricks Flow Schema (nodes + connections), the same shape accepted by <code>/admin/flows/import</code>. Works for flows built entirely by hand in the editor, so they can be round-tripped or version-controlled. This is distinct from the top-level <code>/admin/export</code>, which produces <code>.rbm</code> manifests.
   */
  public FlowImportPayload pull(PullFlowsRequest request) {
    return this.rawClient.pull(request).body();
  }

  /**
   * Export a flow into the Rulebricks Flow Schema (nodes + connections), the same shape accepted by <code>/admin/flows/import</code>. Works for flows built entirely by hand in the editor, so they can be round-tripped or version-controlled. This is distinct from the top-level <code>/admin/export</code>, which produces <code>.rbm</code> manifests.
   */
  public FlowImportPayload pull(PullFlowsRequest request, RequestOptions requestOptions) {
    return this.rawClient.pull(request, requestOptions).body();
  }

  /**
   * Delete a specific flow by its ID.
   */
  public SuccessMessage delete(DeleteFlowRequest request) {
    return this.rawClient.delete(request).body();
  }

  /**
   * Delete a specific flow by its ID.
   */
  public SuccessMessage delete(DeleteFlowRequest request, RequestOptions requestOptions) {
    return this.rawClient.delete(request, requestOptions).body();
  }
}
